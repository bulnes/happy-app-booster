import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// primeiro arquivo a ser renderizado

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
